@model SkyStockPortfolio.Models.Stock

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>Stock</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.stock_name)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.stock_name)
            @Html.ValidationMessageFor(model => model.stock_name)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.stock_last_price)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.stock_last_price)
            @Html.ValidationMessageFor(model => model.stock_last_price)
        </div>

        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
